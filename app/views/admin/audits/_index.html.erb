<div class="ui small modal">
  <i class="close icon"></i>
  <div class="ui header blue">
    Login
  </div>
  <div class="content">
    <table class="ui table">
      <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @audits.each do |audit| %>
        <tr>
          <td><%= audit.id %></td>
          <td><%= audit.created_at.to_s %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
